{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Volty Extension",
            "type": "extensionHost",
            "request": "launch",
            "outFiles": [
                "${workspaceFolder}/dist/**/*.js"
            ],
            "preLaunchTask": "compile"
        }
    ]
}